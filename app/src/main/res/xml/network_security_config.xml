<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Allow HTTP for your local server -->
        <domain includeSubdomains="true">192.168.31.189</domain>
    </domain-config>

    <!-- Allow all other cleartext (optional during dev) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
